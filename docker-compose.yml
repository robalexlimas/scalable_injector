version: "2"
services:
  injector:
    build: injector
    depends_on:
      - controller
    volumes:
      - ./injector/results:/injector/results
  controller:
    build: controller
    ports:
    - "8000:80"
